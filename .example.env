env
# This file is for documentation purposes only.
# To use these variables, create a .env.local file and copy the variables from here.

# Firebase Project Configuration
# These are public and safe to expose on the client-side.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# Firebase Admin (for server-side operations like user creation)
# These are private and should NOT be exposed on the client-side.
# Ensure the private key is a single line with `\n` for newlines.
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY=

# Generative AI API Key (Google AI Studio)
# This is private and should NOT be exposed on the client-side.
GEMINI_API_KEY=

# Application URL
# The full URL of your deployed application (e.g., http://localhost:9002 for local dev)
NEXT_PUBLIC_APP_URL=

# Payment Gateway Keys (Paddle - International)
# These are private.
PADDLE_VENDOR_ID=
PADDLE_API_KEY=

# Payment Gateway Keys (PayFast - Local/Pakistan)
# These are private.
PAYFAST_MERCHANT_ID=
PAYFAST_MERCHANT_KEY=
PAYFAST_PASSPHRASE=

# Email Service (Resend)
# This is private.
RESEND_API_KEY=
